# Відстеження активності компанії

Простий веб-додаток для відстеження активності компанії з можливістю додавання, редагування та видалення записів. Дані зберігаються локально в браузері (localStorage) і можуть бути експортовані в Markdown формат.

## Технології

- **Frontend**: React з TypeScript
- **Зберігання даних**: localStorage (браузер)
- **Експорт**: Markdown формат

## Структура проекту

```
auto-tracking/
├── frontend/         # React frontend
│   ├── src/
│   │   ├── components/
│   │   ├── services/
│   │   ├── types/
│   │   └── App.tsx
│   └── package.json
└── README.md
```

## Встановлення та запуск

### Frontend

1. Перейдіть до директорії frontend:
```bash
cd frontend
```

2. Встановіть залежності:
```bash
npm install
```

3. Запустіть React додаток:
```bash
npm start
```

Frontend буде доступний на `http://localhost:3000`

## Структура даних активності

```typescript
{
  id: number;
  hour: string;              // Година (00-23)
  minute: string;            // Хвилина (00-59)
  participantsCount?: number; // Кількість людей (1-10)
  coordinates?: string;      // Координати
  mainPerson: string;        // Стрім в активності (обов'язкове)
  link?: string;             // Посилання
  comment?: string;          // Коментар
}
```

## Функціональність

- ✅ Перегляд всіх переміщень у вигляді таблиці
- ✅ Додавання нової активності (форма завжди видима)
- ✅ Редагування існуючої активності
- ✅ Видалення активності
- ✅ Автокомпліт для поля "Стрім" (з існуючих переміщень)
- ✅ Валідація обов'язкових полів (час та Стрім)
- ✅ Експорт всіх даних в Markdown формат
- ✅ Локальне зберігання в localStorage
- ✅ Адаптивний дизайн

## Експорт даних

Натисніть кнопку "📥 Експортувати в Markdown" в header, щоб завантажити файл з усіма активностями у форматі Markdown.

Файл буде мати назву `company-activities-YYYY-MM-DD.md` і містити всі активності з усіма деталями.

## Примітки

- Всі дані зберігаються локально в браузері (localStorage)
- Дані зберігаються автоматично при кожній зміні
- Для очищення даних можна очистити localStorage браузера
- Дані не синхронізуються між пристроями або браузерами
